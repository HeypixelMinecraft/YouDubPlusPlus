# YouDub Desktop (PyQt)

This folder contains the desktop UI built with PyQt-Fluent-Widgets.

The desktop app starts the FastAPI backend (uvicorn) as a subprocess, then talks to it via `http://127.0.0.1:<port>/api/...`.

